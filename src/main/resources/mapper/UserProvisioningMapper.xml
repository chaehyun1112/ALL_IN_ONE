<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aio.hospitalsafety.mapper.admin.UserProvisioningMapper">

    <select id="existsUserId" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
            FROM tb_emp
            WHERE emp_id = #{userId}
        )
    </select>

    <select id="existsWardInHospital" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
            FROM tb_ward
            WHERE hosp_div_id = #{hospitalId}
              AND ward_id = #{wardId}
        )
    </select>

    <select id="findThirdFloorWardId" resultType="java.lang.Long">
        SELECT ward_id
        FROM tb_ward
        WHERE hosp_div_id = #{hospitalId}
          AND TRIM(ward_nm) LIKE '3%'
        ORDER BY ward_id
        LIMIT 1
    </select>

    <insert id="insertUser">
        INSERT INTO tb_emp (
            emp_id,
            emp_pw,
            hosp_div_id,
            ward_id,
            emp_nm,
            phone_no,
            room_no,
            job_cd,
            role_cd,
            auth_st,
            must_change_password
        )
        VALUES (
            #{userId},
            #{passwordHash},
            #{hospitalId},
            #{wardId},
            #{userName},
            #{phoneNumber,jdbcType=VARCHAR},
            #{roomNumber,jdbcType=VARCHAR},
            #{jobType},
            'USER',
            'APPROVED',
            TRUE
        )
    </insert>

</mapper>
