<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aio.hospitalsafety.mapper.admin.UserProvisioningMapper">

    <select id="existsUserId" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
            FROM tb_emp
            WHERE emp_id = #{userId}
        )
    </select>

    <select id="existsWardInHospital" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
            FROM tb_ward
            WHERE hosp_div_id = #{hospitalId}
              AND ward_id = #{wardId}
        )
    </select>

    <!-- 실제 DB에는 email 컬럼이 없고 auth_st 기본값이 'APPROVED'라서
         (SignupMapper.xml과 동일하게) 두 컬럼은 INSERT에서 생략한다. -->
    <insert id="insertUser">
        INSERT INTO tb_emp (
            emp_id,
            emp_pw,
            hosp_div_id,
            ward_id,
            emp_nm,
            role_cd
        )
        VALUES (
            #{userId},
            #{passwordHash},
            #{hospitalId},
            #{wardId},
            #{userName},
            'USER'
        )
    </insert>

</mapper>
