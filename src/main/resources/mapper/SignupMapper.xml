<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aio.hospitalsafety.mapper.SignupMapper">

    <!-- 병원 존재 여부 확인 -->
    <select id="existsHospital" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
            FROM tb_hospital
            WHERE hosp_div_id = #{hospDivId}
        )
    </select>

    <!-- 서비스 전체에서 사용자 아이디 중복 확인 -->
    <select id="existsUserId" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
            FROM tb_emp
            WHERE emp_id = #{userId}
        )
    </select>

    <!-- 선택한 병동이 현재 병원 소속인지 확인 -->
    <select id="existsWardInHospital" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
            FROM tb_ward
            WHERE hosp_div_id = #{hospDivId}
              AND ward_id = #{wardId}
        )
    </select>

    <!-- 현재 병원에 속한 병동 목록 조회 -->
    <select id="findWardsByHospital"
            resultType="com.aio.hospitalsafety.dto.WardOption">
        SELECT
            ward_id,
            ward_nm
        FROM tb_ward
        WHERE hosp_div_id = #{hospDivId}
        ORDER BY ward_id
    </select>

    <!-- 사용자 회원가입 정보 저장 -->
    <insert id="insertUser">
        INSERT INTO tb_emp (
            emp_id,
            emp_pw,
            hosp_div_id,
            ward_id,
            emp_nm,
            role_cd,
            auth_st
        )
        VALUES (
            #{userId},
            #{encodedPassword},
            #{hospDivId},
            #{wardId,jdbcType=BIGINT},
            #{userName},
            'USER',
            'PENDING'
        )
    </insert>

</mapper>